services:
  personal-api:
    image: akuan09/personal-api:latest
    container_name: personal-api
    volumes:
      - ./personal-api-data.json:/usr/src/app/data.json
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.pf-backend.rule=Host(`personal-api.asd9.fun`)"
      - "traefik.http.routers.pf-backend.entrypoints=websecure"
      - "traefik.http.routers.pf-backend.tls.certresolver=letsencrypt"
      - "traefik.http.services.pf-backend.loadbalancer.server.port=3000"
    networks:
      - traefik_network

networks:
  traefik_network:
    external: true
